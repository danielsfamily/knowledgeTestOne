// string,number,udefined,boolean,null,BigInt,symbolTask 1 Research
/*array,object,Function
 * The spread operator is a tool that simplifies working with data structures and enhances code readability
 * 1)(Math.round)rounds a number to the nearest integar
  2)(Math.ceil)rounds a nuber up"""""
 3)(Math.floor)rounds a rumber down"""""
  1)length2)push3)pop
 1)length2)trim3)slice
 arrays do not have indexes
  bracket notation
 dot notation
 
   strings..length property  (.length)
   arrays..length property   (.length)
  
   let firstName = "Kevin";
  
   let lastName = "Daniels";

   let lovesToCode= "true";
    const lovesToCode ="false";
/*
 * function fullName(firstName,lastName){
 * fullName = firstName + " " + lastName;
 * return fullName;
 *}
 *  
 * console.log(fullName)
 * 
 * 
 * 
 * const book = {
    title: "Ready Player One",
    author: "Ernest Cline",
    publishedYear: 2011
};
 *const movie = {
    title: "Inception",
    director: "Christopher Nolan",
    releaseYear: 2010
};
 *
 * const student = {
    name: "Kevin",
    age: 49, 
    grades: [85, 90, 92]
};
 * 
 * function findBiggestNumber(num1, num2) {
    return num1 > num2 ? num1 : num2;
}
 * function checkStringsLength(str, maxLength) {
    if (str.length > maxLength) {
        return "Too Long";
    } else {
        return "Within Limit";
    }
}
 * 
 * 
 *   







